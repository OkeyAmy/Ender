# =================================================================
# ender - Environment Variables Configuration
# =================================================================
# Copy this file to .env.local and fill in your API keys
# cp .env.example .env.local
#
# For detailed recommendations and usage instructions, see README.md
# =================================================================

# =================================================================
# REQUIRED - WEB SCRAPING
# =================================================================
# At least ONE scraper API key is required
# Recommendation: Use FIRECRAWL_API_KEY (default, fastest, most reliable)

# Firecrawl API Key (Recommended - Default scraper)
# Get it at: https://firecrawl.dev
# Use case: Fast and reliable web scraping with caching, screenshot support
# Performance: Fast response times, built-in caching reduces API calls
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# ScrapeGraph AI API Key (Optional - Alternative scraper)
# Get it at: https://scrapegraphai.com
# Use case: AI-powered intelligent web scraping, better for complex pages
# Performance: Better for complex pages, may be slower than Firecrawl
# Recommendation: Configure as backup if Firecrawl has issues
# SCRAPEGRAPH_API_KEY=your_scrapegraph_api_key_here

# =================================================================
# REQUIRED - AI PROVIDER
# =================================================================
# At least ONE AI provider API key is required
# Recommendation: Use ANTHROPIC_API_KEY for best quality, or GROQ_API_KEY for fastest speed

# Anthropic API Key (Recommended - Best quality)
# Get it at: https://console.anthropic.com
# Use case: High-quality code generation, best reasoning capabilities
# Performance: Slower but highest quality output
# Recommendation: Best for complex projects requiring high-quality code
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (Recommended - Good balance)
# Get it at: https://platform.openai.com
# Use case: Fast code generation with good quality
# Performance: Fast response times, good quality
# Recommendation: Good balance of speed and quality
# OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Key (Optional - For experimentation)
# Get it at: https://aistudio.google.com/app/apikey
# Use case: Alternative AI provider, experimental features
# Performance: Fast, good for simple tasks
# Recommendation: Good for experimentation, not primary choice
# GEMINI_API_KEY=your_gemini_api_key_here

# Groq API Key (Recommended - Fastest option)
# Get it at: https://console.groq.com
# Use case: Ultra-fast inference, good for rapid iterations
# Performance: Fastest response times, cost-effective
# Recommendation: Best for rapid development and iterations
# GROQ_API_KEY=your_groq_api_key_here

# =================================================================
# OPTIONAL - PERFORMANCE & FEATURES
# =================================================================

# Morph API Key (Recommended - Significantly speeds up code edits)
# Get it at: https://morphllm.com/dashboard
# Use case: Faster code application for edits (not initial generation)
# Performance: Can reduce edit time by 50-70%
# Recommendation: Highly recommended if you make frequent edits
# Note: Only used for edits, not initial code generation
# MORPH_API_KEY=your_morphllm_api_key_here

# Vercel AI Gateway API Key (Optional - For production deployments)
# Get it at: https://vercel.com/docs/ai/ai-gateway
# Use case: Unified API access, rate limiting, caching, analytics
# Performance: Better rate limiting and caching, single API key for all providers
# Recommendation: Use in production for better API management
# Note: When set, overrides individual provider API keys
# AI_GATEWAY_API_KEY=your_ai_gateway_api_key_here

# Application Base URL (Recommended - Required for production)
# Default: http://localhost:3000
# Use case: Production deployments, custom domains
# Example: https://your-domain.com or https://your-app.vercel.app
# Recommendation: Must be set for production deployments
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =================================================================
# REQUIRED - SANDBOX PROVIDER
# =================================================================
# Choose ONE sandbox provider: 'vercel' (recommended) or 'e2b'
# Recommendation: Use 'vercel' for most users, 'e2b' for advanced use cases

SANDBOX_PROVIDER=vercel

# =================================================================
# VERCEL SANDBOX CONFIGURATION
# =================================================================
# Required if SANDBOX_PROVIDER=vercel
# Choose ONE authentication method (Method A recommended for development)

# Method A: OIDC Token (Recommended for development)
# Get it: Run `vercel link` then `vercel env pull` in your project
# Use case: Development environments, automatic token management
# Performance: No manual token management needed
# Recommendation: Easiest setup, recommended for development
VERCEL_OIDC_TOKEN=auto_generated_by_vercel_env_pull

# Method B: Personal Access Token (For production or when OIDC unavailable)
# Get VERCEL_TOKEN at: https://vercel.com/account/tokens
# Get VERCEL_TEAM_ID: Found in team settings URL or API
# Get VERCEL_PROJECT_ID: Found in project settings
# Use case: Production environments, CI/CD pipelines
# Performance: More control, better for automated deployments
# Recommendation: Use for production deployments
# VERCEL_TEAM_ID=team_xxxxxxxxx
# VERCEL_PROJECT_ID=prj_xxxxxxxxx
# VERCEL_TOKEN=vercel_xxxxxxxxxxxx

# =================================================================
# E2B SANDBOX CONFIGURATION
# =================================================================
# Required if SANDBOX_PROVIDER=e2b
# Get it at: https://e2b.dev
# Use case: Longer-running builds, more control over environment
# Performance: 30-minute timeout vs 15 minutes for Vercel
# Recommendation: Use for advanced users needing longer timeouts
# E2B_API_KEY=your_e2b_api_key_here

# =================================================================
# OPTIONAL - DEVELOPMENT & DEBUGGING
# =================================================================

# Debug Mode (Optional - For troubleshooting)
# Default: Not set (no debug logs)
# Use case: Troubleshooting issues, development debugging
# Example: Set to any value to enable (e.g., DEBUG=true)
# Recommendation: Enable when debugging issues
# DEBUG=false

# NPM Install Flags (Recommended - Helps with package installation issues)
# Default: Empty string
# Use case: Handle peer dependency conflicts
# Example: --legacy-peer-deps or --force
# Recommendation: Use --legacy-peer-deps to resolve common conflicts
# Performance: Can resolve package installation issues
NPM_FLAGS=--legacy-peer-deps

# Auto-restart Vite (Recommended - Improves development experience)
# Default: Not set (manual restart required)
# Use case: Automatic dev server refresh after installing packages
# Example: true to enable
# Recommendation: Enable for better development experience
# Performance: Reduces manual steps, faster iteration
AUTO_RESTART_VITE=true

# =================================================================
# OPTIONAL - ADVANCED CONFIGURATION
# =================================================================
# Only configure if using custom API endpoints or proxies

# Custom Anthropic API Base URL (Optional)
# Default: https://api.anthropic.com/v1
# Use case: Custom API endpoints, proxies, or self-hosted solutions
# Example: https://custom-proxy.com/v1
# Recommendation: Only set if using custom endpoint
# ANTHROPIC_BASE_URL=https://api.anthropic.com/v1

# Custom OpenAI API Base URL (Optional)
# Default: Standard OpenAI endpoint
# Use case: Custom API endpoints, proxies, or self-hosted solutions
# Example: https://custom-proxy.com/v1
# Recommendation: Only set if using custom endpoint
# OPENAI_BASE_URL=
